misogi:
  grpc:
    host: localhost
    port: 50051
    tls:
      enabled: false
  upload:
    chunk-size: 65536

server:
  port: 8080

logging:
  level:
    com.example.misogi: DEBUG
    io.grpc: WARN
